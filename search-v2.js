(() => {
  const paragraph2Html = `
<section class="section-card searchable" data-search="§ 2 строение газообразных жидких твердых твёрдых тел агрегатное состояние газ жидкость твердое тело твёрдое тело идеальный газ молекулы кинетическая потенциальная энергия тепловое расширение ангстрем">
  <h3>§ 2. Строение газообразных, жидких и твёрдых тел</h3>

  <h4>Три агрегатных состояния вещества</h4>
  <p>Одно и то же вещество в зависимости от условий может находиться в <b>твёрдом, жидком или газообразном состоянии</b>. Например: лёд, вода и водяной пар.</p>
  <table class="compact-table">
    <tr><th>Состояние</th><th>Форма и объём</th><th>Как расположены частицы</th></tr>
    <tr><td><b>Твёрдое тело</b></td><td>Сохраняет форму и объём.</td><td>Частицы очень близко друг к другу и в основном колеблются около устойчивых положений.</td></tr>
    <tr><td><b>Жидкость</b></td><td>Не имеет собственной формы, принимает форму сосуда; объём почти не меняется.</td><td>Частицы расположены близко, но могут перемещаться друг относительно друга.</td></tr>
    <tr><td><b>Газ</b></td><td>Не имеет ни собственной формы, ни собственного объёма; заполняет весь сосуд.</td><td>Частицы находятся далеко друг от друга и свободно хаотически движутся.</td></tr>
  </table>

  <div class="important">
    <div class="tag">Важно</div>
    <p><b>Агрегатное состояние вещества определяется</b> взаимным расположением его молекул, а также характером их <b>движения и взаимодействия</b>.</p>
  </div>

  <h4>Расстояния и силы между молекулами</h4>
  <ul>
    <li>Для очень малых размеров часто используют <b>ангстрем</b>: <b>1 Å = 10⁻¹⁰ м</b>.</li>
    <li>Если расстояние между молекулами во много раз больше их размеров, силы взаимодействия очень малы.</li>
    <li>При уменьшении расстояния заметно усиливается <b>притяжение</b>.</li>
    <li>Когда расстояние становится сравнимым с размерами молекул, резко возрастает <b>отталкивание</b>. Поэтому жидкости и твёрдые тела трудно сжать.</li>
  </ul>

  <h4>Движение и энергия молекул</h4>
  <p>Молекулы непрерывно движутся, поэтому обладают <span class="term">кинетической энергией</span>. Так как молекулы взаимодействуют, они обладают и <span class="term">потенциальной энергией</span>.</p>
  <p>Потенциальная энергия зависит от расстояния между молекулами. В устойчивом равновесном положении силы притяжения и отталкивания компенсируют друг друга, а потенциальная энергия минимальна.</p>

  <h4>Физическая модель идеального газа</h4>
  <p>В обычных условиях расстояния между молекулами газа намного больше размеров самих молекул, поэтому их собственными размерами и силами взаимодействия часто можно пренебречь.</p>
  <div class="important">
    <div class="tag">Модель идеального газа</div>
    <p>Молекулы считают <b>точечными телами</b>, которые почти не взаимодействуют друг с другом, непрерывно и хаотически движутся и сталкиваются, меняя направление движения.</p>
  </div>

  <h4>Твёрдые тела и жидкости</h4>
  <p>В жидкостях и твёрдых телах молекулы находятся значительно ближе друг к другу, чем в газах, поэтому силы их взаимодействия сильнее.</p>
  <ul>
    <li><b>Жидкость:</b> молекулы близко расположены и могут перемещаться — поэтому жидкость течёт и принимает форму сосуда.</li>
    <li><b>Твёрдое тело:</b> частицы сильно связаны и в основном колеблются около устойчивых положений — поэтому тело сохраняет форму.</li>
  </ul>

  <h4>Тепловое расширение</h4>
  <p>При нагревании большинство тел <b>расширяется</b>, а при охлаждении — <b>уменьшается в размерах</b>. Это связано с изменением движения частиц и расстояний между ними.</p>
  <p>Тепловое расширение учитывают в быту и технике: провода линий электропередачи подвешивают с небольшим провисанием, а между рельсами и другими длинными конструкциями оставляют промежутки.</p>
  <p>При нагревании газа расстояние между его молекулами увеличивается, объём растёт, а плотность уменьшается. На этом основан подъём нагретого воздуха, например в воздушных шарах.</p>

  <div class="conclusion">
    <div class="tag">Вывод</div>
    <p>Свойства газов, жидкостей и твёрдых тел объясняются <b>расположением, движением и взаимодействием молекул</b>. Тепловое расширение также объясняется положениями молекулярно-кинетической теории.</p>
  </div>

  <h4>Что нужно запомнить</h4>
  <table class="compact-table">
    <tr><th>Тема</th><th>Коротко</th></tr>
    <tr><td>Газ</td><td>Нет своей формы и объёма; молекулы далеко друг от друга.</td></tr>
    <tr><td>Жидкость</td><td>Сохраняет объём, принимает форму сосуда; молекулы близко и подвижны.</td></tr>
    <tr><td>Твёрдое тело</td><td>Сохраняет форму и объём; частицы сильно связаны.</td></tr>
    <tr><td>1 Å</td><td>10⁻¹⁰ м.</td></tr>
    <tr><td>Идеальный газ</td><td>Молекулы считают точечными, их взаимодействием почти пренебрегают.</td></tr>
    <tr><td>Нагревание</td><td>Обычно приводит к увеличению размеров тела.</td></tr>
  </table>

  <h4>Ответы на вопросы после § 2</h4>
  <div class="qa">
    <div class="q">1. Чем объясняются различия между газообразными, жидкими и твёрдыми телами?</div>
    <div class="answer">Различия объясняются тем, как расположены молекулы, как они движутся и насколько сильно взаимодействуют друг с другом.</div>
  </div>
  <div class="qa">
    <div class="q">2. Что происходит с твёрдыми телами, жидкостями и газами при нагревании?</div>
    <div class="answer">Большинство твёрдых тел, жидкостей и газов при нагревании расширяется, потому что изменяются движение частиц и средние расстояния между ними.</div>
  </div>
  <div class="qa">
    <div class="q">3. Приведите примеры использования теплового расширения тел в быту и в технике.</div>
    <div class="answer">Провода ЛЭП подвешивают с провисанием, между рельсами и длинными конструкциями оставляют зазоры. Расширение нагретого газа используют при подъёме воздушных шаров.</div>
  </div>
</section>`;

  if (!document.querySelector('[data-paragraph="2"]') && ![...document.querySelectorAll('.searchable h3')].some(h => /^\s*§\s*2(?:\.|\s|$)/i.test(h.textContent))) {
    const unitCard = [...document.querySelectorAll('.searchable')].find(card => /перевод единиц/i.test(card.dataset.search || ''));
    if (unitCard) {
      const holder = document.createElement('div');
      holder.innerHTML = paragraph2Html.trim();
      const p2 = holder.firstElementChild;
      p2.dataset.paragraph = '2';
      unitCard.before(p2);
    }
  }

  const input = document.getElementById('searchInput');
  const note = document.getElementById('resultNote');
  const cards = [...document.querySelectorAll('.searchable')];
  if (!input || !note || !cards.length) return;

  const normalize = (s) => (s || '').toLowerCase().replace(/ё/g, 'е').trim();

  function clearHighlights() {
    document.querySelectorAll('mark.search-hit').forEach(mark => {
      mark.replaceWith(document.createTextNode(mark.textContent));
    });
    document.body.normalize();
  }

  function escapeRegExp(s) {
    return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  }

  function highlightTerms(root, terms) {
    const cleanTerms = [...new Set(terms.map(t => t.trim()).filter(Boolean))].sort((a, b) => b.length - a.length);
    if (!cleanTerms.length) return [];
    const regex = new RegExp(`(${cleanTerms.map(escapeRegExp).join('|')})`, 'gi');
    const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, {
      acceptNode(node) {
        if (!node.nodeValue.trim()) return NodeFilter.FILTER_REJECT;
        const parent = node.parentElement;
        if (!parent || ['SCRIPT', 'STYLE', 'MARK'].includes(parent.tagName)) return NodeFilter.FILTER_REJECT;
        return NodeFilter.FILTER_ACCEPT;
      }
    });
    const nodes = [];
    while (walker.nextNode()) nodes.push(walker.currentNode);
    const hits = [];
    nodes.forEach(node => {
      const text = node.nodeValue;
      regex.lastIndex = 0;
      if (!regex.test(text)) return;
      regex.lastIndex = 0;
      const frag = document.createDocumentFragment();
      let last = 0;
      text.replace(regex, (match, _group, offset) => {
        frag.appendChild(document.createTextNode(text.slice(last, offset)));
        const mark = document.createElement('mark');
        mark.className = 'search-hit';
        mark.textContent = match;
        frag.appendChild(mark);
        hits.push(mark);
        last = offset + match.length;
        return match;
      });
      frag.appendChild(document.createTextNode(text.slice(last)));
      node.replaceWith(frag);
    });
    return hits;
  }

  function paragraphByNumber(number) {
    return cards.find(card => {
      const heading = card.querySelector('h3');
      return heading && new RegExp(`^\\s*§\\s*${escapeRegExp(number)}(?:\\.|\\s|$)`, 'i').test(heading.textContent);
    }) || null;
  }

  function scrollToTarget(target) {
    if (!target) return;
    setTimeout(() => target.scrollIntoView({ behavior: 'smooth', block: 'center' }), 20);
  }

  function smartSearch() {
    clearHighlights();
    const raw = input.value.trim();
    const q = normalize(raw);
    if (!q) {
      cards.forEach(card => card.classList.remove('hidden'));
      note.textContent = '';
      return;
    }

    if (/^\d+$/.test(q)) {
      const card = paragraphByNumber(q);
      cards.forEach(item => item.classList.toggle('hidden', item !== card));
      if (!card) {
        note.textContent = `Параграф § ${q} не найден`;
        return;
      }
      const heading = card.querySelector('h3') || card;
      const hits = highlightTerms(heading, [q]);
      note.textContent = `Параграф § ${q}`;
      scrollToTarget(hits[0] || heading);
      return;
    }

    const words = q.split(/\s+/).filter(Boolean);
    const visibleCards = [];
    cards.forEach(card => {
      const text = normalize(card.innerText + ' ' + (card.dataset.search || ''));
      const match = words.every(word => text.includes(word));
      card.classList.toggle('hidden', !match);
      if (match) visibleCards.push(card);
    });

    const hits = [];
    visibleCards.forEach(card => hits.push(...highlightTerms(card, words)));
    if (hits.length) {
      note.textContent = `Совпадений: ${hits.length}`;
      scrollToTarget(hits[0]);
    } else if (visibleCards.length) {
      note.textContent = `Найдено разделов: ${visibleCards.length}`;
      scrollToTarget(visibleCards[0]);
    } else {
      note.textContent = 'Ничего не найдено';
    }
  }

  input.addEventListener('input', smartSearch);

  const style = document.createElement('style');
  style.textContent = `mark.search-hit{background:#fff1a8;color:inherit;border-radius:4px;padding:0 2px;box-shadow:0 0 0 1px rgba(111,74,168,.12)}`;
  document.head.appendChild(style);
})();
